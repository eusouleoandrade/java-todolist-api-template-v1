paginationSettings:
  maxPageSize: 50
  defaultPageSize: 10
  initialPagination: 1

management:
  endpoints:
    web:
      exposure:
        include: health,info

spring:
  datasource:
    url: jdbc:mysql://127.0.0.1:3306/todolistdb
    username: root
    password: pwdmysql
    driver-class-name: com.mysql.cj.jdbc.Driver